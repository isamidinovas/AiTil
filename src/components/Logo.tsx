import { Link } from "lucide-react";
import LogoImg from "../../public/Лого Aitil.svg";

const Logo = () => (
  <div className="flex items-center justify-center gap-3 mb-8 select-none">
    <img src={LogoImg} />
  </div>
);

export default Logo;
